# PostgreSQL container environment variables
# These will be written to db_connection.txt by startup.sh when the DB starts.

POSTGRES_HOST=localhost
POSTGRES_PORT=5000
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123
POSTGRES_DB=myapp

# Optional unified URL preferred by backends
# Example: postgresql://appuser:dbuser123@localhost:5000/myapp
POSTGRES_URL=
